import * as React from 'react';
import * as ReactDOM from 'react-dom';
import BackgroundWindow from './background-window/background-window';

ReactDOM.render(<BackgroundWindow />, document.getElementById('ReactContainer'));
